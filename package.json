{
  "name": "moneyagenda-web",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/sitemap-hreflang.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "assets": "node scripts/assets.mjs",
    "test": "node tests/vectors.test.mjs",
    "verify": "npm run build && node scripts/check-links.mjs && npm test && node scripts/check-text.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "^3.6.0",
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "astro": "^5.14.1",
    "sharp": "^0.34.4"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
